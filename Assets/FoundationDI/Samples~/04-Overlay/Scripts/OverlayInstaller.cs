using Reflex.Core;

namespace DarkNaku.FoundationDI.Samples
{
    public class OverlayInstaller : SampleInstaller
    {
        public override void InstallBindings(ContainerBuilder builder)
        {
            base.InstallBindings(builder);
        }
    }
}
